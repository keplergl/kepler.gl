import {FC} from 'react';

type Props = {
  absolute?: boolean;
  top?: number | string;
  right?: number | string;
  bottom?: number | string;
  left?: number | string;
};

const Position: FC<Props>;
export default Position;
